import { Logo } from "../../utils";
import { ListToggle } from "./ListToggle";
export const Navbar = () => {
  return (
    <header className="header">
      <Logo />
      <ListToggle />
    </header>
  );
};
